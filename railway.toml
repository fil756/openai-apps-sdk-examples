[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm run serve & uvicorn pizzaz_server_python.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "pizzaz-mcp"
